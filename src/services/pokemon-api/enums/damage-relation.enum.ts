 enum DamageRelation {
  NEUTRAL = 'neutral',
  RESISTANT = 'resistant',
  VULNERABLE = 'vulnerable',
  TWICE_RESISTANT = 'twice_resistant',
  IMMUNE = 'immune',
  TWICE_VULNERABLE = 'twice_vulnerable',
}

export default DamageRelation;
